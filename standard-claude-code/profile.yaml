name: standard-claude-code
displayName: Standard Claude Code
runtime: claude
model: anthropic/claude-sonnet-4-20250514

skills: []

planning:
  strategy: react
  maxSteps: 50

cost:
  maxTokensPerTurn: 16000
  maxTotalTokens: 200000

emptyState:
  welcomeTitle: Standard Claude Code agent
  welcomeSubtitle: A generic task agent guided by the current user or delegated task prompt.

a2a:
  enabled: true
  name: standard-claude-code
  description: A generic Claude Code style agent for delegated task execution or review prompts.
  submitArtifacts: true
  invocationMode: task-delegation
  historyPolicy: task-thread
  workspacePolicy: same-project
  artifactPolicy: visible
